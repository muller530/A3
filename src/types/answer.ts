export interface Answer {
  record_id: string;
  question: string;
  standard_answer: string;
  enable_status: string;
  scene: string;
  tone: string;
  product_name: string;
  product_id: string;
}

